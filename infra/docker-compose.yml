services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_USER: dashr
      POSTGRES_PASSWORD: dashr
      POSTGRES_DB: dashr
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
